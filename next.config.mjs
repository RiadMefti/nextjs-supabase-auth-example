/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "luymfotygsqyuavekgpe.supabase.co",
      },
    ],
  },
};

export default nextConfig;
